production:
  driver: postgres
  open: user=dali dbname=dali_production sslmode=disable

development:
  driver: postgres
  open: user=dali dbname=dali_development sslmode=disable

test:
  driver: postgres
  open: user=dali dbname=dali_testing sslmode=disable
